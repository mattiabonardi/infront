import { IRadioButtons } from "@/types/components/widget";

/**
 * Radio Buttons component
 * @param props
 * @returns
 */
export const RadioButtons: React.FC<IRadioButtons> = (props) => {
  console.log("RADIO BUTTONS", props);
  return <div>RADIO BUTTON</div>;
};
