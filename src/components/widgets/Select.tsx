import { ISelect } from "@/types/components/widget";

/**
 * Select component
 * @param props
 * @returns
 */
export const Select: React.FC<ISelect> = (props) => {
  console.log("SELECT", props);
  return <div>SELECT</div>;
};
