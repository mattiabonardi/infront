import { ITree } from "@/types/components/widget";

/**
 * Tree component
 * @param props
 * @returns
 */
export const Tree: React.FC<ITree> = (props) => {
  console.log("TREE", props);
  return <div>TREE</div>;
};
